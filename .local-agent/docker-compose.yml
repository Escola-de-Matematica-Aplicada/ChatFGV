services:
  agents:
    build:
      context: .
    working_dir: /workspaces
    volumes:
      - "C:/Users/julio.chaves/PycharmProjects/Escola-de-Matematica-Aplicada/pergunta-aos-dados:/workspaces"
      - "/var/run/docker.sock:/var/run/docker.sock"  # acesso ao Docker da máquina host
    extra_hosts:
      - "host.docker.internal:host-gateway"